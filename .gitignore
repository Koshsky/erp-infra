.env

SECURITY_AUDIT.md

*.pid
*.pid.lock
docker-compose.override.yml
*.log

*.crt
*.key
*.pem
*.p12
*.pfx

.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

*.tmp
*.temp
temp/
tmp/
backups/
# Локальные кэши сборки Electron (создаются при упаковке desktop/)
.electron-cache/
.npm-cache/
.xdg-cache/
# Локальный кэш Go-сборки (используется сборочной средой)
.gocache/
# Локальные кэши Go-модулей/пути и линтера (writable-обходы read-only module cache)
.gomodcache/
.gopath/
.lintcache/
# Локальный кэш Docker (используется сборочной средой, см. DOCKER_CONFIG)
.docker-cfg/
